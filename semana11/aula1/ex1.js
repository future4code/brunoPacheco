const numUm = Number(process.argv[2]);
const numDois = Number(process.argv[3]);
const numTres = Number(process.argv[4]);

const somaNumero = numUm + numDois + numTres;

console.log("O valor total é:", somaNumero );